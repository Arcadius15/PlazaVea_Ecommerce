<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.pe.idat.pva">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlazaVeaApp"
        tools:targetApi="31">
        <activity
            android:name=".activities.CambiarContraActivity"
            android:exported="false" />
        <activity
            android:name=".activities.pasarela.ResumenActivity"
            android:exported="false" />
        <activity
            android:name=".activities.pasarela.EscogerTipoActivity"
            android:exported="false" />
        <activity
            android:name=".activities.RegTarjetaActivity"
            android:exported="false" />
        <activity
            android:name=".activities.RegRucActivity"
            android:exported="false" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBZiA_WchgnQhaPfto26tbBX6N2eM1OuX8" />

        <activity
            android:name=".activities.DireccionRegistroActivity"
            android:exported="false"
            android:label="@string/title_activity_direccion_registro" />
        <activity
            android:name=".activities.shopping_bag.ShoppingBagActivity"
            android:exported="false"
            android:label="@string/title_activity_carrito" />
        <activity
            android:name=".activities.products.detail.ProductsDetailActivity"
            android:exported="false" />
        <activity
            android:name=".activities.products.ProductsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.HomeActivity"
            android:exported="false"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.PlazaVeaApp.NoActionBar" />
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>